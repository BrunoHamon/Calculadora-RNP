#include <stdio.h>
#include <libprg/libprg.h>

int main(int argc, char *argv[]) {
    //Chama a fução de calculadora, usando o numero de argumentos e seus valores, via linha de comando.
    notacao_polonesa_inversa(argc, argv);

    return 0;
}
